version: 2.1
orbs:
  heroku: circleci/heroku@4.1.0
  node: circleci/node@1.2.3
workflows:
  test-and-deploy:
    jobs:
      - node/test
      - heroku/deploy-via-git:
        filters:
          branches:
            only:
              - master
        requires:
          - node/test
